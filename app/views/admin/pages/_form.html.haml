= form_for [:admin, @page], html: {class: "pure-form"} do |f|
  - if @page.errors.any?
    #error_explanation
      %h2= "#{pluralize(@page.errors.count, "error")} prohibited this page from being saved:"
      %ul
        - @page.errors.full_messages.each do |msg|
          %li= msg
  %fieldset
    .pure-control-group
      = f.label :title
      = f.text_field :title, size: "60x1"
    .pure-control-group
      = f.label :body
      %br
      = f.cktext_area :body, :class => 'someclass'
    .pure-control-group
      = f.label 'tags'
      = f.text_field :all_tags, placeholder: "Tags", size: "60x1"
    .actions
      = f.submit 'Save', class: "admin-button__large"
